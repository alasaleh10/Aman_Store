import 'package:flutter/material.dart';

class NoLocationContiner extends StatelessWidget {
  const NoLocationContiner({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}